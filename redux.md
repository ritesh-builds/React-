redux is a different thing and redux-toolkit is different...
redux is a core library and react-redux is is the implementation of that library.

reducers = functions
and 
features = slices   

reducers is the object but it has all the functions/methods

every app have only one store.
